{% extends 'base.html.twig' %}

{% block title %}Tests - {{ parent() }}{% endblock %}

{% block stylesheets %}

{% endblock %}

{% block body %}
    {#Serializer test#}
    {{ dump(serialized) }}
    <hr>
    Json Response test
    {{ dump(jsonresponse) }}
    <hr>
    Meals relation test
    {% for meal in restaurant.meals %}
        {{ dump(meal.foodname) }}
    {% endfor %}
    <hr>
    Review relation test
    {% for review in restaurant.reviews %}
        {{ dump(review.comment) }}
    {% endfor %}
    <hr>
    All restaurants name test
    {% for singleRestaurant in restaurants %}
        {{ dump(singleRestaurant.name) }}
    {% endfor %}
{% endblock %}
